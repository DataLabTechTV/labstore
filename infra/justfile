set shell := ["bash", "-uc"]

set dotenv-load
set dotenv-required
set dotenv-path := "../.env"

default:
    just -l

up:
    cd .. && docker compose -p labstore -f infra/compose.yml up --build -d

down:
    cd .. && docker compose -p labstore -f infra/compose.yml down -v
